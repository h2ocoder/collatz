# Stopping Offset
**Source:** Paper 2, Section 3

The position within a [[Stopping Page]], analogous to a memory offset. 0-indexed.

**Example:** For class 8: $n=11$ has offset 0, $n=23$ has offset 1.

**Code:** `collatz.stopping.stopping_offset(n)`
