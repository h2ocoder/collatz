[features]
seeds = false
skip-lint = false

[programs.localnet]
bounty-escrow = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[programs.devnet]
bounty-escrow = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

# ---------------------------------------------------------------------------
# Note: after first `anchor build`, run `anchor keys list` to get the real
# program ID and replace the placeholder above in both this file and
# programs/bounty-escrow/src/lib.rs.
# ---------------------------------------------------------------------------
