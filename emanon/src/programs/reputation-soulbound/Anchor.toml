[features]
seeds = false
skip-lint = false

[programs.localnet]
reputation-soulbound = "RptnSbnd111111111111111111111111111111111111"

[programs.devnet]
reputation-soulbound = "RptnSbnd111111111111111111111111111111111111"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

# ---------------------------------------------------------------------------
# Note: after first `anchor build`, run `anchor keys list` to get the real
# program ID and replace the placeholder above (in both this file and
# programs/reputation-soulbound/src/lib.rs).
# ---------------------------------------------------------------------------
